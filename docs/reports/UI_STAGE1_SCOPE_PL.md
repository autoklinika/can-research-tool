# UI Stage 1 — scope guard

Ten etap obejmuje wyłącznie klasyczną powłokę IDE, układ docków, nawigację,
statusy i spójny motyw QSS. Zmiany funkcjonalne w komunikacji CAN, zapisie,
filtrach i dekoderach są poza zakresem.
